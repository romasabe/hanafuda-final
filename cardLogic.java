import java.util.Random;
import java.util.ArrayList;
public class cardLogic
{
    public static void main(String[] args)
    {
        static void create(int[] fillArray){
            for (int q = 1; q >)
        }
        static void initialize(numberofCards, handSize){
            int randomNumber;
            ArrayList<Cards> unplayed = new ArrayList<Cards>();
            ArrayList<int> played = new ArrayList<int>();
            ArrayList<int> handPOne = new ArrayList<int>();
            ArrayList<int> handPTwo = new ArrayList<int>();
            for (int i = 0; i <= handSize*2; i++){
                boolean drawAttempt = false;
                while (drawAttempt == false){
                    randomNumber = nextInt(48);
                    for (int e = 0; e > played.length){
                        //if () if statement will check for if the array contains the deck index.
                    }
                    
                    

                }
                

                //handPOne[i] = 
            }

            

        }
       
    }
}